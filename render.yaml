services:
  - type: worker
    name: server-utilities
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m scripts.migrate_legacy && python server_utilities.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: DATABASE_URL
        sync: false
